apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eks-cicd-demo.fullname" . }}-test-connection"
  labels:
    {{- include "eks-cicd-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eks-cicd-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
